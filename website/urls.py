from django.urls import path, include
from django.contrib import admin
from website.views import *

urlpatterns = [
    # path('signup/', signup_page, name='signupPage'),
    # path('', first_page, name='firstPage'),
]

